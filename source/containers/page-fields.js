var objectKeys = require('object-keys')
var xtend = require('xtend')
var html = require('choo/html')

var blueprintDefault = require('../blueprints/default.json')

module.exports = pageFields

function pageFields (state, emit) {
  
}
